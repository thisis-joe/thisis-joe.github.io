---
title: 04. Thread Pool Starvation 상세
created: 2026-07-01T00:06:00+09:00
updated: 2026-07-01T00:06:00+09:00
---

>[!success] 이 문서를 통해 아래 질문들에 답할 수 있게 됩니다.
>- `Thread Pool Starvation` 관점에서 어떤 실행 비용을 봐야 하는가?
>- 장애 상황에서 어떤 OS/JVM 지표를 먼저 확인하는가?
>- Java/Spring 코드와 운영 지표를 어떻게 연결하는가?

## 개요

이 문서의 주제는 `Thread Pool Starvation`이다. Thread pool이 모두 대기 작업으로 묶여 새 작업을 처리하지 못하는 장애 패턴을 다룬다.

먼저 붙잡을 기준은 다음 문장이다. pool 내부 대기는 전체 처리 중단으로 번질 수 있다. 코드가 정상적으로 보여도 실제 실행 환경에서는 CPU, thread, heap, file descriptor, socket, disk, GC 같은 자원이 함께 움직인다. 따라서 이 문서는 개념 설명에서 멈추지 않고 관찰 명령과 장애 판단 기준까지 연결한다.

## 핵심 모델

- 판단 기준: pool 내부 대기는 전체 처리 중단으로 번질 수 있다.
- Blocking은 현재 thread가 결과를 기다리는 동안 다른 작업을 처리하지 못하는 실행 방식이다.
- Non-blocking은 대기 중인 I/O를 OS나 event loop에 맡기고 thread 점유를 줄이는 방식이지만 CPU 작업 자체가 사라지지는 않는다.
- Async는 호출자와 실행자를 분리한다. 실행자가 부족하거나 queue가 무제한이면 병목 위치만 바뀐다.
- Event loop는 적은 thread로 많은 연결을 다루지만 loop 안에서 blocking 호출이나 긴 CPU 작업을 하면 연결 전체가 지연된다.
- 실전 선택은 유행보다 외부 I/O 시간, 동시 연결 수, 팀의 디버깅 능력, backpressure 설계에 따라 달라진다.

## 문서별 핵심 구분

- pool의 모든 worker가 대기 작업에 묶이면 새 작업이 실행되지 못한다.
- 같은 pool에 작업을 제출하고 결과를 기다리는 구조가 위험하다.
- starvation은 CPU가 낮은데 latency가 커지는 형태로 자주 보인다.

## 원리

Thread pool starvation은 worker가 모두 바쁜 상태를 넘어, worker들이 새 작업이나 완료 작업이 실행되기를 기다리면서 pool 내부가 스스로 막히는 패턴이다.

대표적인 구조는 같은 pool 안에서 부모 작업이 자식 작업을 제출하고 `get()`으로 기다리는 경우다. 자식 작업을 실행할 빈 worker가 없으면 부모 worker들은 모두 기다리고, queue에는 실행되지 못한 자식 작업이 쌓인다.

이 장애는 CPU가 낮은데 latency가 커지는 형태로 자주 나타난다. thread들은 계산을 하는 것이 아니라 `Future.get`, `CountDownLatch.await`, queue take, connection acquire 같은 위치에서 기다린다.

`Thread Pool Starvation` 관점에서 중요한 점은 "worker 수 부족"과 "pool 안 의존성"을 구분하는 것이다. 단순히 pool size를 키우면 잠시 완화될 수 있지만, 같은 구조가 남아 있으면 더 큰 규모에서 다시 막힌다.

## Java/Spring 연결

- Spring MVC blocking 모델은 단순하고 thread dump로 읽기 쉽지만 동시 blocking I/O가 많으면 worker thread가 빠르게 찬다.
- WebFlux나 Netty 기반 처리는 event loop를 막지 않는다는 전제가 깨지면 MVC보다 더 넓게 지연이 번질 수 있다.
- `CompletableFuture.supplyAsync`는 호출을 비동기로 만들지만, 내부가 blocking이면 결국 executor thread를 점유한다.
- DB JDBC driver는 일반적으로 blocking I/O이므로 reactive Controller를 써도 DB 구간에서 별도 전략이 필요하다.
- Timeout, queue size, bulkhead, retry backoff가 없으면 async 구조는 장애를 흡수하지 못하고 증폭할 수 있다.

## 코드와 설정 예시

```java
Future<Order> child = executor.submit(() -> orderClient.fetch(orderId));

// 같은 executor의 worker 안에서 다시 같은 executor 작업을 기다리면 starvation 후보가 된다.
return child.get();
```

예시는 같은 pool 안에서 작업을 제출하고 다시 기다리는 구조를 보여 준다. 모든 worker가 이런 대기 상태에 빠지면 queue에 있는 완료 작업이나 자식 작업이 실행될 기회를 잃는다.

## 관찰 명령

```bash
jcmd <pid> Thread.print
curl -s localhost:8080/actuator/metrics/executor.queued
curl -s localhost:8080/actuator/metrics/executor.active
```

운영 환경에서는 명령도 부하가 될 수 있다. dump, JFR, 대량 `lsof`, 긴 로그 출력은 범위와 시간을 제한하고, 수집 시각과 대상 PID를 함께 남긴다.

## 지표 해석

| 신호 | 먼저 의심할 것 | 다음 확인 |
| --- | --- | --- |
| Tomcat busy thread가 max 근처 | blocking I/O 또는 긴 요청 처리 | thread dump와 endpoint별 latency |
| executor queue가 계속 증가 | worker 부족 또는 downstream 지연 | active/queued/rejected 지표 |
| event loop thread CPU가 높음 | loop 안 CPU 작업 또는 blocking 호출 | JFR, thread name, stack trace |
| timeout 후 retry 급증 | backpressure 실패 | retry 로그와 downstream error rate |

지표는 같은 시간축에서 비교해야 한다. 예를 들어 API latency가 먼저 올랐는지, GC pause가 먼저 늘었는지, pool pending이 먼저 증가했는지에 따라 원인과 결과가 달라진다.

## 장애 상황에서 보는 순서

1. 요청 thread가 어디서 멈췄는지 thread dump로 본다.
2. 외부 API, DB, file I/O 호출 시간이 전체 latency에서 차지하는 비율을 분리한다.
3. Executor active, queue, rejected 지표를 확인한다.
4. Async 작업이 같은 pool에 다시 작업을 제출하며 기다리는 구조가 있는지 찾는다.
5. 비필수 작업은 rate limit, circuit breaker, 기능 차단으로 먼저 줄인다.

이 순서는 절대 규칙이 아니다. 이미 명확한 증거가 있다면 해당 계층부터 보되, 결론을 내리기 전에는 Application, JVM, OS 중 최소 두 계층의 증거를 맞춘다.

## 실전 팁

- dump에서 같은 executor worker가 `Future.get`, `join`, `await`에 몰려 있는지 본다.
- parent task와 child task가 같은 pool을 쓰는지 코드 경로를 확인한다.
- 사용자 요청, batch, callback, scheduler는 가능한 한 pool을 분리한다.
- queue depth와 active worker가 동시에 높은데 completed가 늘지 않으면 starvation을 의심한다.
- pool size 증가는 임시 완화로만 보고 의존 구조를 제거한다.

## 주니어 팁

- `Thread Pool Starvation`을 단어로만 외우지 말고, 정상 상태와 장애 상태에서 같은 명령을 실행해 차이를 비교한다.
- 처음에는 `top`, `ps`, `ss`, `jcmd Thread.print`, Actuator metric처럼 재현 가능한 기본 도구부터 익힌다.
- 숫자가 크거나 작다는 판단보다 평소 기준선에서 얼마나 벗어났는지를 먼저 본다.

## 시니어 팁

- `Thread Pool Starvation`에서 생긴 병목이 다른 pool, queue, retry, timeout으로 어떻게 전파되는지 설계 단계에서 확인한다.
- 장애 대응 runbook에는 명령뿐 아니라 해석 기준과 금지할 대응도 같이 적는다.
- 단일 인스턴스 기준의 해결책이 다중 인스턴스, container, LB 환경에서도 유효한지 확인한다.

## Guru급 팁

- snapshot 하나보다 여러 시점의 샘플이 강하다. thread dump, CPU, memory, socket 상태를 같은 간격으로 2~3회 수집하면 상태 전이가 보인다.
- JFR, async-profiler, eBPF, perf 같은 도구는 결론을 장식하는 도구가 아니라 가설을 검증하는 도구다.
- 병목을 제거하면 병목은 사라지는 것이 아니라 다른 자원으로 이동할 수 있다. 수정 후 새로운 saturation 지점을 다시 확인한다.

## 위험 신호!

- 모든 worker가 `CompletableFuture.join()`이나 `Future.get()`에서 멈춰 있다.
- executor queue는 쌓이는데 처리 완료 수가 늘지 않는다.
- CPU 사용률은 낮은데 요청 timeout이 증가한다.
- 공용 executor를 여러 기능이 공유해 어느 작업이 굶기는지 모른다.
- rejection이 없어서 장애가 빠르게 실패하지 않고 오래 대기한다.

## 확인 질문

- `Thread Pool Starvation`에서 Application 로그만 보면 부족한 이유는 무엇인가?
	- Application 로그는 증상과 코드 경로를 보여 주지만 CPU, memory, socket, disk, scheduler 같은 하위 자원의 포화 여부를 직접 설명하지 못하기 때문이다.
- 장애 중 설정값을 바로 키우기 전에 확인해야 할 것은 무엇인가?
	- 어떤 자원이 먼저 포화됐는지, 하위 시스템 capacity가 남아 있는지, 변경이 장애를 완화하는지 증폭하는지 확인해야 한다.
- 같은 증상이 재발하지 않게 하려면 무엇을 남겨야 하는가?
	- 발생 시간, 영향 범위, 사용한 명령, 핵심 지표, 원인 판단, 완화 조치, 근본 수정, 남은 리스크를 남겨야 한다.

## 참고 문서

- [Java 21 API, ExecutorService](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/ExecutorService.html)
- [Oracle Java 21 Tools, jcmd](https://docs.oracle.com/en/java/javase/21/docs/specs/man/jcmd.html)
- [Spring Boot Reference, Actuator](https://docs.spring.io/spring-boot/reference/actuator/index.html)
- [Spring Boot Reference, Metrics](https://docs.spring.io/spring-boot/reference/actuator/metrics.html)
- [Brendan Gregg, USE Method](https://www.brendangregg.com/usemethod.html)
